CREATE TABLE sales (
    order_id VARCHAR(50),
    order_date DATE,
    status VARCHAR(50),
    fulfillment VARCHAR(50),
    sales_channel VARCHAR(50),
    category VARCHAR(100),
    size VARCHAR(20),
    quantity INT,
    amount DECIMAL(10,2),
    city VARCHAR(100),
    state VARCHAR(100)
);
